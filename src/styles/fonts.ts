import { Nunito, Ropa_Sans } from 'next/font/google';

export const ropa = Ropa_Sans({
  subsets: ['latin'],
  weight: ['400'],
});

export const nunito = Nunito({
  subsets: ['latin'],
  weight: ['400', '700'],
});
